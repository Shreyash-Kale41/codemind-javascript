var bankName;
console.log(`before initialization bank name : ${bankName} and datatype is ${typeof(bankName)}`);
bankName="SBI Bank";
console.log(`After initailization bank name : ${bankName} and type is ${typeof(bankName)}`);

var marks="90%";
console.log(`Marks : ${marks} and datatype is ${typeof(marks)}`);

var isWorking;
console.log(`before initialization : ${isWorking} and datatype is ${typeof(isWorking)}`);
isWorking=true;
console.log(`After initailization : ${isWorking} and datatype is ${typeof(isWorking)}`);

var totalCount="Hundred and seven";
console.log(`Total Count is ${totalCount} and datatype is ${typeof(totalCount)}`);
totalCount=107;
console.log(`Total Count is ${totalCount} and datatype is ${typeof(totalCount)}`);

